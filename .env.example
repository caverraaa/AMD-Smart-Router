# Local development only — the grading harness injects the real values.
FIREWORKS_API_KEY=fw_your_key_here
FIREWORKS_BASE_URL=https://api.fireworks.ai/inference/v1
# Comma-separated IDs; the harness replaces this with its exact allowlist.
ALLOWED_MODELS=accounts/fireworks/models/gpt-oss-120b
# Optional: force the model pick (must be a member of ALLOWED_MODELS)
# CHEAP_MODEL=
